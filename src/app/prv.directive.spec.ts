import { PrvDirective } from './prv.directive';

describe('PrvDirective', () => {
  it('should create an instance', () => {
    const directive = new PrvDirective();
    expect(directive).toBeTruthy();
  });
});
